# PR 描述：Section 15 / 24 - SARSA（State-Action-Reward-State-Action）

## 1. 背景与目标
本次提交基于 HTB Academy 第 15 节内容，新增一份完整的本地 PR 描述文档，系统梳理 SARSA（State-Action-Reward-State-Action）算法的核心思想、数学更新规则、on-policy 学习机制、探索策略、收敛条件与参数调优要点，以及算法适用前提与关键假设，便于后续复习、面试准备与工程迁移。

## 2. 变更范围
### 新增文件
- `PR_DESCRIPTION_SARSA.md`

### 变更类型
- 文档补充（学习内容结构化沉淀）
- 不涉及业务代码、模型训练脚本、推理流程或系统配置修改

## 3. 主要内容概览
### 3.1 SARSA 定义与定位
- SARSA 是一种时序差分（Temporal-Difference, TD）控制算法。
- 名称来自一次更新中使用的五元组：`State -> Action -> Reward -> State -> Action`。
- 学习目标是逼近动作价值函数 `Q(s, a)`，并据此改进策略。

### 3.2 更新规则（核心公式）
SARSA 的单步更新可写为：

`Q(S_t, A_t) <- Q(S_t, A_t) + α [R_{t+1} + γ Q(S_{t+1}, A_{t+1}) - Q(S_t, A_t)]`

其中：
- `α`：学习率（learning rate）
- `γ`：折扣因子（discount factor）
- `R_{t+1}`：在状态 `S_t` 执行动作 `A_t` 后获得的即时回报
- `Q(S_{t+1}, A_{t+1})`：在下一状态中，按当前行为策略实际选到的动作价值

要点：
- TD 目标依赖“下一步真实执行动作”的 Q 值，而非下一状态最大 Q 值。
- 更新是增量式、在线式的，能够边交互边学习。

### 3.3 On-policy 学习机制
- SARSA 属于 **on-policy**：学习的目标策略与行为策略一致。
- 这意味着在更新时使用的是“实际采取的下一动作 `A_{t+1}`”。
- 在含探索行为（如 ε-greedy）的训练过程中，SARSA 学到的价值天然反映探索风险与真实行为分布。

### 3.4 与 Off-policy 思路的直觉差异
- SARSA：对“当前策略下真的会执行的动作”进行评估与更新，策略更保守、风险意识更强。
- Off-policy（如 Q-learning 常见写法）：常使用 `max_a Q(s', a)` 作为目标，更偏向理想贪心目标。

> 本文档聚焦 SARSA 本身，比较内容仅用于帮助理解 on-policy 特性。

### 3.5 探索策略与实践
常见探索策略：
- ε-greedy：以 `1-ε` 概率选择当前最优动作，以 `ε` 概率随机探索。
- 可配合 ε 衰减：训练前期高探索、后期低探索。

实践建议：
- 过高 ε：学习噪声大，收敛慢。
- 过低 ε：容易陷入局部次优，覆盖不足。
- 应结合环境复杂度和训练轮次设计 ε 与衰减曲线。

### 3.6 收敛与参数调优要点
影响收敛与最终性能的关键超参数：
- 学习率 `α`：过大易震荡，过小收敛慢。
- 折扣因子 `γ`：决定对长期回报的重视程度。
- 探索参数 `ε` 与衰减节奏：决定探索-利用平衡。

经验性调优方向：
- 在早期观察回报曲线趋势，识别高方差或停滞问题。
- 分阶段调整 `α`/`ε`，必要时降低学习率并减缓策略变化。
- 使用多次随机种子评估稳定性，避免单次结果偶然性。

### 3.7 算法假设与适用前提
SARSA 应用时通常默认：
- 可与环境持续交互并获得逐步奖励反馈；
- 状态与动作空间可离散化或可用函数逼近承载；
- 训练阶段允许探索行为；
- 任务目标可通过长期累计回报进行刻画。

在高风险场景中，SARSA 因 on-policy 特性通常更能反映“含探索的真实执行风险”。

## 4. 学习价值与工程意义
- 建立从 Bellman 思想到 TD 控制算法的连续认知链路。
- 强化对 on-policy / off-policy 差异的工程直觉。
- 为后续实现表格型强化学习 baseline、以及迁移到近似函数方法提供概念基础。
- 为后续章节（如 Q-learning、更高级控制算法）提供对照参照。

## 5. 验证说明
本次变更为文档新增，验证重点如下：
- 文件成功创建且命名符合既有约定；
- 内容覆盖 Section 15 SARSA 的核心知识点；
- 仅新增文档，无其他源码或配置文件副作用。

## 6. 风险评估
- 代码风险：无（不涉及代码执行路径）
- 运行风险：无（不涉及运行配置变更）
- 维护风险：低（纯文档，结构清晰，可持续迭代）

## 7. 后续建议
- 后续可补充一个最小可运行 SARSA 伪代码示例，增强“公式 -> 实现”的映射。
- 可在后续章节文档中追加 SARSA 与 Q-learning 的实验对比记录（如收敛速度、稳定性、风险偏好）。

## 8. 提交摘要（TL;DR）
- 新增 `PR_DESCRIPTION_SARSA.md`，完整沉淀 SARSA 章节学习内容；
- 覆盖定义、更新规则、on-policy 机制、探索策略、收敛调参与假设前提；
- 仅文档变更，不影响现有代码与运行行为。
