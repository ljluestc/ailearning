# PR 描述：Section 18 / 24 - Neural Networks

## 1. 背景与目标
本次提交基于 HTB Academy「Fundamentals of AI」第 18 节内容，新增一份完整的本地 PR 描述文档，系统整理神经网络（尤其是多层感知机 MLP）的核心概念与训练机制，覆盖网络结构、神经元计算、激活函数、反向传播与梯度下降等关键知识点，便于后续复习与知识沉淀。

## 2. 变更范围
### 新增文件
- `PR_DESCRIPTION_NEURAL_NETWORKS.md`

### 变更类型
- 文档新增（学习内容结构化归档）
- 不涉及任何业务代码、训练脚本、配置或依赖修改

## 3. 主要内容概览
### 3.1 多层感知机（MLP）总体结构
文档说明了神经网络用于克服单层感知机局限的核心思路，并将网络划分为三个主要部分：
- 输入层（Input Layer）
- 一个或多个隐藏层（Hidden Layers）
- 输出层（Output Layer）

通过多层结构与非线性激活，MLP 能学习复杂的非线性决策边界。

### 3.2 神经元（Neuron）计算机制
每个神经元作为基本计算单元，执行以下流程：
1. 接收来自上一层的输入；
2. 对输入做加权求和；
3. 加上偏置项（bias）；
4. 通过激活函数得到输出。

相比使用阶跃函数的传统感知机，神经元可选择 sigmoid、ReLU、tanh、softmax 等函数，表达能力更强。

### 3.3 输入层、隐藏层、输出层职责
#### 输入层
- 每个输入节点通常对应一个特征；
- 负责将原始特征传入网络，不做复杂变换。

#### 隐藏层
- 接收前一层全部节点输入并进行非线性变换；
- 多层隐藏层可学习层级化特征：前层偏基础模式，后层偏高阶抽象。

#### 输出层
- 二分类任务常用 1 个输出神经元（常搭配 sigmoid）；
- 多分类任务常为每个类别配置 1 个输出神经元（常搭配 softmax）。

### 3.4 多层网络的表达能力
文档强调了 MLP 相比单层模型的关键优势：
- 能学习非线性关系；
- 能解决如 XOR 等线性不可分问题；
- 通过层次化表示提升模型表达能力与泛化潜力。

### 3.5 激活函数角色与常见类型
激活函数用于引入非线性，是神经网络有效学习复杂模式的关键。文档整理了常见函数：
- **Sigmoid**：输出范围 (0,1)，历史常用，但存在梯度消失风险；
- **ReLU**：负值置零、正值线性，训练快且实践中广泛使用；
- **Tanh**：输出范围 (-1,1)，相较 sigmoid 以 0 为中心；
- **Softmax**：常用于多分类输出层，将分数映射为概率分布。

### 3.6 MLP 训练流程：反向传播 + 梯度下降
文档将训练拆分为两部分：

#### 反向传播（Backpropagation）
- 前向传播得到预测值；
- 通过损失函数计算预测误差；
- 按链式法则自输出层向前逐层求梯度；
- 产出各层权重与偏置的梯度信息。

#### 梯度下降（Gradient Descent）
- 随机初始化参数；
- 使用反向传播梯度更新参数；
- 按学习率控制步长，持续迭代直到收敛或达到迭代上限。

两者协同实现“误差驱动参数优化”，使模型逐步提升预测性能。

## 4. 核心知识要点总结
- 神经网络通过“多层 + 非线性”突破线性模型能力上限；
- 神经元计算遵循“线性变换 + 激活函数”范式；
- 输出层设计依赖任务类型（二分类/多分类）；
- 激活函数选择直接影响训练稳定性与收敛速度；
- 训练本质是利用反向传播求梯度，再由梯度下降更新参数。

## 5. 学习价值与工程意义
- 为后续深度学习模块（CNN/RNN/Transformer 等）打下统一的训练认知基础；
- 建立从“模型结构”到“优化过程”的完整思维链路；
- 对理解超参数（如学习率）及训练稳定性问题（如梯度消失）有直接帮助。

## 6. 验证说明
本次为文档新增，验证重点如下：
- 文件已创建且命名符合仓库既有 PR 描述命名惯例；
- 内容覆盖 Section 18 关键知识（结构、激活、训练）；
- 无源码、配置与依赖层面的副作用。

## 7. 风险评估
- 代码风险：无（纯文档提交）
- 运行风险：无（不影响执行路径）
- 维护风险：低（内容结构化清晰，便于后续扩展）

## 8. 后续建议
- 可补充一个最小 MLP 数值示例（含前向与一次反向更新）增强“公式到实现”的可解释性；
- 可在后续章节增加不同激活函数训练曲线对比，强化参数与收敛行为理解。

## 9. 提交摘要（TL;DR）
- 新增 `PR_DESCRIPTION_NEURAL_NETWORKS.md`；
- 完整沉淀 Section 18 神经网络核心内容；
- 覆盖 MLP 结构、神经元计算、激活函数、反向传播、梯度下降；
- 仅文档变更，不影响现有代码与运行。
