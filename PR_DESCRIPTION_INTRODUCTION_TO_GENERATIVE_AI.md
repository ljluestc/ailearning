# PR 描述：Section 21 / 24 - Introduction to Generative AI

## 1. 背景与目标
本次提交基于 HTB Academy「Fundamentals of AI」第 21 节内容，新增一份完整的本地 PR 描述文档，系统整理生成式 AI（Generative AI）的基础概念、工作机制、常见模型类型与核心评估指标，帮助建立从“原理认知”到“实践判断”的统一知识框架。

## 2. 变更范围
### 新增文件
- `PR_DESCRIPTION_INTRODUCTION_TO_GENERATIVE_AI.md`

### 变更类型
- 文档新增（课程知识结构化沉淀）
- 不涉及业务代码、训练脚本、配置项或依赖变更

## 3. 主要内容概览
### 3.1 生成式 AI 的定义与定位
文档明确了生成式 AI 与传统判别式 AI 的核心差异：
- 传统 AI 更关注识别、分类、预测；
- 生成式 AI 更关注“创造新内容”，例如文本、图像、音乐、代码等。

核心思想是：模型学习训练数据分布后，能够生成与人类创作风格相似且具备一定原创性的内容。

### 3.2 生成式 AI 的基本工作流程
文档将流程分为三个关键阶段：

1. **Training（训练）**
   - 使用大规模样本学习数据中的统计关系与结构规律；
   - 捕捉“什么样的数据是合理的”。

2. **Generation（生成）**
   - 从随机种子或潜变量出发；
   - 按学习到的分布逐步生成新样本。

3. **Evaluation（评估）**
   - 评估生成结果的质量、多样性、与真实分布的接近程度；
   - 可采用主观评估（人工）与客观指标（自动化度量）结合。

### 3.3 常见生成模型类型
文档归纳了四类主流范式：

#### GAN（Generative Adversarial Networks）
- 由生成器与判别器对抗训练；
- 通过博弈提升生成样本真实性；
- 优势：图像合成效果常见优秀；
- 挑战：训练不稳定、易出现模式崩塌。

#### VAE（Variational Autoencoders）
- 学习压缩潜在表示（latent representation）；
- 可从潜在空间采样并重构新样本；
- 优势：潜在空间结构较规整、可控性较好。

#### 自回归模型（Autoregressive Models）
- 按序列逐步生成（基于前文生成后文）；
- 常用于文本生成任务；
- 优势：序列建模直观、概率解释性强。

#### 扩散模型（Diffusion Models）
- 先对数据加噪至接近纯噪声；
- 再学习逆过程，从噪声逐步去噪生成样本；
- 优势：近年来在高质量图像生成中表现突出。

## 4. 关键概念解析
### 4.1 Latent Space（潜在空间）
- 是数据在低维/压缩语义空间中的表示；
- 相似样本通常在潜在空间中距离更近；
- 通过潜在空间采样可实现可控生成与插值探索。

### 4.2 Sampling（采样）
- 指从学习到的数据分布中抽样并映射到输出空间；
- 采样策略直接影响生成结果的质量与多样性；
- 模型是否“学到有效分布”决定采样上限。

### 4.3 Mode Collapse（模式崩塌）
- 常见于 GAN：生成器只产出少量模式，缺乏多样性；
- 即使训练数据丰富，输出仍可能“千篇一律”。

### 4.4 Overfitting（过拟合）
- 模型过度记忆训练样本与噪声，泛化能力下降；
- 在生成任务中会抑制原创性，生成结果可能趋同或“抄样本”。

## 5. 评估指标与质量判断
文档总结了常见指标：
- **Inception Score (IS)**：关注图像清晰度与类别多样性；
- **Fréchet Inception Distance (FID)**：衡量生成分布与真实分布距离，越低通常越好；
- **BLEU（文本任务）**：衡量生成文本与参考文本的匹配程度与流畅性。

同时强调：单一指标不足以覆盖“真实性 + 多样性 + 可用性”，应结合任务目标进行综合评估。

## 6. 核心结论与学习价值
- 生成式 AI 的核心不在“记忆样本”，而在“学习分布并生成新样本”；
- 模型选择需结合任务形态（图像/文本/多模态）、可控性需求与训练稳定性；
- 质量评估需平衡客观指标与主观体验，避免仅看单一分数；
- 该节内容为后续深入 LLM、扩散模型应用与生成系统工程化实践奠定基础。

## 7. 验证说明
本次为文档新增提交，验证点如下：
- 文件命名与仓库既有 PR 描述文件规范保持一致；
- 覆盖 Section 21 关键知识点（流程、模型、概念、指标）；
- 未引入代码、配置、依赖层面的任何变更。

## 8. 风险评估
- 代码风险：无（纯文档变更）
- 运行风险：无（不影响任何执行逻辑）
- 维护风险：低（结构化内容，便于后续迭代补充）

## 9. 后续建议
- 可追加“GAN vs Diffusion vs Autoregressive”的任务场景对照小结，增强选型能力；
- 可补充“指标失真案例”（如高分但主观质量差）以提升评估实践意识；
- 可在后续章节结合简单实验记录（prompt、采样参数、输出对比）形成可复用模板。

## 10. 提交摘要（TL;DR）
- 新增 `PR_DESCRIPTION_INTRODUCTION_TO_GENERATIVE_AI.md`；
- 系统沉淀 Section 21 生成式 AI 入门内容；
- 覆盖定义、工作流程、主流模型、关键概念与评估指标；
- 仅文档变更，不影响现有代码与运行行为。
