# PR 描述：Section 20 / 24 - Recurrent Neural Networks

## 1. 背景与目标
本次提交基于 HTB Academy「Fundamentals of AI」第 20 节内容，新增一份完整的本地 PR 描述文档，系统梳理循环神经网络（RNN）的序列建模机制、隐藏状态记忆、训练难点（梯度消失）、改进结构（LSTM/GRU）及双向 RNN 的上下文建模能力，帮助建立“序列数据特性 -> 网络结构 -> 训练挑战 -> 架构改进”的完整认知路径。

## 2. 变更范围
### 新增文件
- `PR_DESCRIPTION_RECURRENT_NEURAL_NETWORKS.md`

### 变更类型
- 文档新增（学习内容结构化沉淀）
- 不涉及业务代码、训练脚本、部署配置或依赖调整

## 3. 主要内容概览
### 3.1 RNN 的问题定位与核心价值
- RNN 专门面向**序列数据**建模，适用于“顺序影响语义”的任务场景；
- 相比前馈网络一次性独立处理输入，RNN 通过递归连接保留历史信息；
- 典型应用包括：自然语言处理、语音识别、时间序列分析等。

### 3.2 序列处理机制：输入 + 隐藏状态
每个时间步 RNN 模块接收两类输入：
1. 当前时刻输入 `x_t`（如当前单词）；
2. 上一时刻隐藏状态 `h_{t-1}`（历史上下文摘要）。

并产生两类输出：
1. 当前时刻输出 `y_t`；
2. 新的隐藏状态 `h_t`（传递给下一时间步）。

该循环链路使模型能够在序列推进过程中持续累积上下文信息。

### 3.3 隐藏状态的“记忆”作用
- 隐藏状态可理解为对历史输入的压缩表示；
- 序列越往后，模型可利用更丰富的上下文辅助预测；
- 例如处理句子时，后续词预测可利用前文语义线索，提高一致性。

### 3.4 训练难点：梯度消失问题
RNN 在使用 BPTT（Backpropagation Through Time）训练时，存在典型梯度消失风险：
- 梯度在时间维度反向传播时反复连乘；
- 当梯度绝对值小于 1 时，连乘后迅速衰减至接近 0；
- 导致远距离时间步对当前损失的影响难以回传，模型难以学习长程依赖。

### 3.5 LSTM：通过门控增强长期记忆
LSTM 在 RNN 基础上引入记忆单元与三类门控机制：
- 输入门（Input Gate）：控制新信息写入；
- 遗忘门（Forget Gate）：控制旧信息保留/丢弃；
- 输出门（Output Gate）：控制记忆信息输出。

该机制可显著缓解梯度消失，使网络更稳定地建模长期依赖关系。

### 3.6 GRU：更轻量的门控循环单元
GRU 通过更简化结构实现与 LSTM 相近能力，主要包含：
- 更新门（Update Gate）：控制历史状态保留比例；
- 重置门（Reset Gate）：控制历史信息与当前输入融合方式。

在许多任务上，GRU 在参数规模与训练效率方面更具优势。

### 3.7 双向 RNN：融合前后文信息
- 标准 RNN 仅按单方向（通常从左到右）处理序列；
- 双向 RNN 同时进行正向与反向建模，并融合两个方向隐藏状态；
- 在整段序列可用的任务中（如文本标注），双向建模通常更充分利用上下文。

## 4. 学习价值与工程意义
- 明确 RNN 在序列任务中的基础地位及与前馈网络的本质差异；
- 建立“普通 RNN -> LSTM/GRU -> 双向建模”的演化认知框架；
- 为后续学习 Seq2Seq、Attention、Transformer 等序列建模方法提供过渡基础。

## 5. 验证说明
本次变更为文档新增，验证重点如下：
- 文件命名遵循仓库既有 PR 描述规范；
- 内容覆盖 Section 20 关键知识点（结构、机制、问题与改进）；
- 仅新增文档，无代码与配置副作用。

## 6. 风险评估
- 代码风险：无（不涉及执行路径）
- 运行风险：无（不涉及训练/部署配置）
- 维护风险：低（内容结构化，便于后续增量维护）

## 7. 后续建议
- 可补充最小 RNN/LSTM/GRU 对比示例，增强“理论 -> 实现”映射；
- 可增加长依赖任务上的性能对照（普通 RNN vs LSTM/GRU）；
- 可在后续章节补充与 Attention 机制的衔接动机。

## 8. 提交摘要（TL;DR）
- 新增 `PR_DESCRIPTION_RECURRENT_NEURAL_NETWORKS.md`；
- 完整沉淀 Section 20 RNN 章节学习内容；
- 覆盖序列处理、隐藏状态、梯度消失、LSTM/GRU 与双向 RNN；
- 仅文档变更，不影响现有代码与运行行为。
