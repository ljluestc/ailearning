# PR 描述：Section 16 / 24 - Q-Learning

## 1. 背景与目标
本次提交基于 HTB Academy 第 16 节 Q-Learning 学习内容，新增一份完整的本地 PR 描述文档，系统梳理 Q-Learning 的算法定位、更新公式、训练流程、探索-利用权衡、关键超参数与适用假设，帮助形成“概念 -> 公式 -> 过程 -> 实践调参”的连贯认知链路，为后续进阶到深度强化学习（如 DQN）打基础。

## 2. 变更范围
### 新增文件
- `PR_DESCRIPTION_Q_LEARNING.md`

### 变更类型
- 文档补充（学习内容结构化沉淀）
- 不涉及业务代码、训练脚本、推理流程或系统配置修改

## 3. 主要内容概览
### 3.1 Q-Learning 定义与定位
- Q-Learning 是一种 **model-free、off-policy** 的时序差分控制算法。
- 学习目标是逼近最优动作价值函数 `Q*(s, a)`，并导出最优策略 `π*(s) = argmax_a Q*(s, a)`。
- 核心思想：无需显式建模环境转移概率，通过与环境交互产生的经验迭代更新 Q 值。

### 3.2 Q-Table 与价值表达
- Q-Table 可理解为状态-动作二维映射：每个单元存储在状态 `s` 执行动作 `a` 的长期回报估计。
- 在离散状态/动作空间中，Q-Table 提供可解释且直接可更新的值函数表示方式。
- 策略改进通常基于当前 Q 值贪心选择，即偏好较大 Q 值的动作。

### 3.3 核心更新公式（Bellman 最优思想）
Q-Learning 单步更新公式为：

`Q(S_t, A_t) <- Q(S_t, A_t) + α [R_{t+1} + γ max_a Q(S_{t+1}, a) - Q(S_t, A_t)]`

其中：
- `α`：学习率（learning rate），控制新信息覆盖旧估计的幅度；
- `γ`：折扣因子（discount factor），控制长期回报权重；
- `R_{t+1}`：即时奖励；
- `max_a Q(S_{t+1}, a)`：下一状态的最大可达动作价值估计。

关键点：
- 更新目标使用“下一状态最大 Q 值”，体现 off-policy 特性；
- 与 SARSA 使用 `Q(S_{t+1}, A_{t+1})`（实际动作）不同，Q-Learning更偏向理想贪心目标。

### 3.4 算法流程与训练闭环
典型流程可归纳为：
1. 初始化 Q-Table（常见为全 0 或小随机值）；
2. 在当前状态按策略（如 ε-greedy）选择动作；
3. 执行动作，观察奖励与下一状态；
4. 按 Q-Learning 公式更新 `Q(S_t, A_t)`；
5. 状态迁移并循环迭代，直到回合结束或达到训练停止条件；
6. 训练后采用贪心策略进行部署/评估。

### 3.5 探索与利用（Exploration vs Exploitation）
- 常用 ε-greedy：
  - 以 `ε` 概率随机探索；
  - 以 `1-ε` 概率选择当前估值最优动作。
- 实践中常配合 ε 衰减：前期强调探索，后期提升利用，帮助在覆盖状态空间与收敛稳定性间取得平衡。

### 3.6 超参数与收敛实践
主要调参维度：
- 学习率 `α`：过大易震荡，过小学习慢；
- 折扣因子 `γ`：越大越重视长期收益；
- 探索率 `ε` 及衰减策略：决定搜索广度与后期策略稳定性。

实践建议：
- 观察每回合累计回报、收敛速度与波动性；
- 分阶段调节 `α` 与 `ε`，避免“过早贪心”或“长期高噪声探索”；
- 使用多随机种子重复实验，降低偶然性结论风险。

### 3.7 适用前提与局限
适用前提（典型）：
- 可与环境持续交互并获得逐步反馈；
- 状态与动作空间可离散化，或可迁移到函数逼近形式；
- 任务目标可由长期累计回报刻画。

局限与延展：
- 在大规模连续空间下，表格法存储与泛化能力不足；
- 常需要迁移到近似函数方法（如 DQN）以提升可扩展性。

## 4. 学习价值与工程意义
- 建立强化学习控制算法入门基线，明确 Q-Learning 在 RL 体系中的位置。
- 强化对 Bellman 更新、off-policy 学习与 ε-greedy 机制的实践理解。
- 为后续与 SARSA、DQN、Policy Gradient 等方法对比学习提供统一参照系。

## 5. 验证说明
本次变更为文档新增，验证重点如下：
- 文件创建成功且命名符合仓库既有约定；
- 内容覆盖 Q-Learning 章节核心知识点（定义、公式、流程、调参与假设）；
- 仅新增文档，无代码与配置副作用。

## 6. 风险评估
- 代码风险：无（不涉及执行路径）
- 运行风险：无（不涉及部署或配置变更）
- 维护风险：低（纯文档，结构化程度高，便于后续增量迭代）

## 7. 后续建议
- 可补充一个最小可运行 Q-Learning 伪代码/示例环境（如 GridWorld）以增强“公式 -> 实现”映射；
- 增加与 SARSA 的对照实验记录（收敛速度、稳定性、风险偏好）；
- 补充从 Q-Table 向 DQN 迁移的动机与关键设计差异。

## 8. 提交摘要（TL;DR）
- 新增 `PR_DESCRIPTION_Q_LEARNING.md`，完整沉淀 Q-Learning 章节学习内容；
- 覆盖算法定位、更新公式、训练流程、探索策略、调参与适用前提；
- 仅文档变更，不影响现有代码与运行行为。
